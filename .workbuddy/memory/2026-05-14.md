# 2026-05-14 工作日志

## 发帖组件重构：贴吧搜索选择 + 创建功能

### 问题
- 发帖弹窗的贴吧 `<select>` 下拉框无法正常显示选项（数据加载后渲染异常）
- 用户需要搜索贴吧的能力，以及创建新贴吧的入口

### 后端改动 (`tb-sawakso-backend/src/index.ts`)
- **新增 `GET /api/bars/search?q=xxx`**：模糊搜索贴吧（name/description LIKE），无参数时返回热门（按 post_count DESC 前20）
- **新增 `POST /api/bars`**：创建贴吧接口（name + description），含重名检测、30字符限制

### 前端改动
- **`src/request/api/bars.js`**：新增 `search(q)` 和 `create(data)` 方法
- **`src/components/CreatePost/CreatePost.vue`** 大幅改造：
  - 移除 `<select>` 下拉框，改为搜索输入框 + 下拉候选列表 UI
  - 支持关键词搜索（300ms 防抖）、键盘上下箭头导航 + Enter 选择
  - 搜索无匹配时显示「创建贴吧「xxx」」选项
  - 点击创建弹出内嵌小弹窗，填名称+简介，创建成功后自动选中
  - 已选中的贴吧显示为蓝色标签（可点击 × 更换）
  - 打开弹窗时预加载热门贴吧列表
  - 点击外部自动关闭下拉

### 构建：0 错误通过
